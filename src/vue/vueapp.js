import Vue from 'vue';

/**
 * @exports
 * New Vue instance to host all vue components. Passed
 * as parent to all main level Vue components.
 * @type {Vue}
 */
export default new Vue({
    el: '#vue-domain',
    template: '<div id="vue-domain"></div>',
});
